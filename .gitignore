# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# GSD planning
.planning/

# Go binaries (the compiled binary at root, not the source directory)
/kaptanto
/kaptanto-*

# Bench binaries
bench/loadgen
bench/reporter
bench/collector
bench/scenarios
bench/statsd
bench/cmd/scenarios/scenarios

# Ad-hoc test output
result.json

# Bench generated HTML reports and raw metrics (large files, not committed)
bench/results/report.html
bench/results/*.jsonl

# Build artifacts
*.o
*.a
*.so

# Vendor directory (using Go modules instead)
vendor/

# Test artifacts
*.test
*.prof
*.out

# Local data directory (kaptanto runtime state)
data/
!landing/src/data/
