---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - go.mod
  - go.sum
  - .gitignore
  - internal/event/event.go
  - internal/event/event_test.go
  - internal/event/ulid.go
  - internal/event/ulid_test.go
  - internal/logging/logging.go
  - internal/logging/logging_test.go
autonomous: true
requirements: [EVT-01, EVT-02, OBS-03]

must_haves:
  truths:
    - "ChangeEvent struct serializes to JSON with all required fields: id, idempotency_key, timestamp, source, operation, table, key, before, after, metadata"
    - "ULID generator produces monotonically increasing, time-ordered IDs safe for concurrent use"
    - "Structured JSON logs are emitted at configurable levels (debug, info, warn, error)"
    - "Insert events have before:null, delete events have after:null, updates have both populated"
  artifacts:
    - path: "internal/event/event.go"
      provides: "ChangeEvent struct with JSON tags and Operation constants"
      contains: "type ChangeEvent struct"
    - path: "internal/event/ulid.go"
      provides: "Thread-safe monotonic ULID generator"
      contains: "type IDGenerator struct"
    - path: "internal/logging/logging.go"
      provides: "slog JSON handler setup with level parsing"
      contains: "func Setup"
  key_links:
    - from: "internal/event/event.go"
      to: "github.com/oklog/ulid/v2"
      via: "ulid.ULID type for ID field"
      pattern: "ulid\\.ULID"
    - from: "internal/event/ulid.go"
      to: "internal/event/event.go"
      via: "generates IDs used in ChangeEvent.ID"
      pattern: "IDGenerator"
---

<objective>
Initialize the Go project and define the core event types, ULID generation, and structured logging that every subsequent phase depends on.

Purpose: Establish the shared data types and logging infrastructure. The ChangeEvent struct is the universal currency of the entire pipeline -- every component produces or consumes it.
Output: Go module with event package (ChangeEvent, Operation, IDGenerator) and logging package (slog JSON setup).
</objective>

<execution_context>
@/Users/lucasandrade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lucasandrade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Initialize Go module and implement ChangeEvent types with ULID generation</name>
  <files>go.mod, go.sum, .gitignore, internal/event/event.go, internal/event/event_test.go, internal/event/ulid.go, internal/event/ulid_test.go</files>
  <behavior>
    - Test: ChangeEvent with OpInsert serializes to JSON with all fields present, before is null, after is populated
    - Test: ChangeEvent with OpDelete serializes with before populated, after is null
    - Test: ChangeEvent with OpUpdate serializes with both before and after populated
    - Test: JSON output contains exact field names: "id", "idempotency_key", "timestamp", "source", "operation", "table", "key", "before", "after", "metadata"
    - Test: IDGenerator.New() produces valid ULIDs
    - Test: Two consecutive IDGenerator.New() calls produce monotonically increasing IDs (id2 > id1 lexicographically)
    - Test: IDGenerator is safe for concurrent use (100 goroutines generating IDs, no panics, all unique)
  </behavior>
  <action>
    1. Initialize Go module: `go mod init github.com/kaptanto/kaptanto` with Go 1.22+
    2. Create .gitignore for Go projects (binaries, vendor/, .DS_Store, data/)
    3. `go get github.com/oklog/ulid/v2@latest github.com/stretchr/testify@latest`
    4. Create internal/event/event.go:
       - Define Operation type (string) with constants: OpInsert="insert", OpUpdate="update", OpDelete="delete", OpRead="read", OpControl="control"
       - Define ChangeEvent struct with JSON tags per research code example. Fields: ID (ulid.ULID), IdempotencyKey (string), Timestamp (time.Time), Source (string), Operation (Operation), Database (string, omitempty), Schema (string, omitempty), Table (string), Key (json.RawMessage), Before (json.RawMessage -- NOT pointer, use null explicitly), After (json.RawMessage), Metadata (map[string]any)
       - Per research pitfall 3: before and after are always present in JSON (null when absent, never omitted). Do NOT use omitempty or pointer for these fields.
    5. Create internal/event/ulid.go:
       - IDGenerator struct with sync.Mutex and *ulid.MonotonicEntropy
       - NewIDGenerator() creates with ulid.Monotonic(rand.Reader, 0)
       - New() method locks mutex, generates ULID with ulid.MustNew(ulid.Timestamp(time.Now()), g.entropy)
       - Per research pitfall 2: single shared entropy source for monotonicity
    6. Write tests first (TDD), then implement to pass them.
  </action>
  <verify>
    <automated>cd /Users/lucasandrade/kaptanto && CGO_ENABLED=0 go test ./internal/event/... -v -count=1</automated>
  </verify>
  <done>ChangeEvent struct serializes to valid JSON with all required fields. ULID generator produces monotonically increasing, unique IDs. All tests pass with CGO_ENABLED=0.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement structured logging with slog JSON handler</name>
  <files>internal/logging/logging.go, internal/logging/logging_test.go</files>
  <behavior>
    - Test: ParseLevel("debug") returns slog.LevelDebug
    - Test: ParseLevel("info") returns slog.LevelInfo
    - Test: ParseLevel("warn") returns slog.LevelWarn
    - Test: ParseLevel("error") returns slog.LevelError
    - Test: ParseLevel("WARNING") returns slog.LevelWarn (case insensitive)
    - Test: ParseLevel("unknown") defaults to slog.LevelInfo
    - Test: Setup() produces JSON-formatted log output (parse output as JSON, verify "msg" and "level" keys exist)
    - Test: Logger at "error" level does not emit "info" messages
  </behavior>
  <action>
    1. Create internal/logging/logging.go:
       - ParseLevel(s string) slog.Level -- switch on strings.ToLower, "debug"->LevelDebug, "warn"/"warning"->LevelWarn, "error"->LevelError, default->LevelInfo
       - Setup(w io.Writer, level string) *slog.Logger -- creates slog.NewJSONHandler with parsed level, sets as slog.SetDefault, returns logger
       - Per research pitfall 4: always explicitly set the handler, never rely on default
    2. Create internal/logging/logging_test.go:
       - Use bytes.Buffer as writer to capture output
       - Verify JSON format by unmarshaling output
       - Verify level filtering works (error-level logger should not emit info messages)
    3. Write tests first (TDD), then implement.
  </action>
  <verify>
    <automated>cd /Users/lucasandrade/kaptanto && CGO_ENABLED=0 go test ./internal/logging/... -v -count=1</automated>
  </verify>
  <done>Structured JSON logging with configurable levels works. ParseLevel handles all valid levels case-insensitively. Setup produces JSON output to configurable writer. All tests pass.</done>
</task>

</tasks>

<verification>
All tests pass with pure Go build:
```bash
CGO_ENABLED=0 go test ./... -v -count=1
```
</verification>

<success_criteria>
- go.mod exists with Go 1.22+ and ulid/testify dependencies
- ChangeEvent struct has all 10 required fields with correct JSON tags
- ULID generator is thread-safe with monotonic entropy
- Structured JSON logging with level parsing works
- All tests pass with CGO_ENABLED=0
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
