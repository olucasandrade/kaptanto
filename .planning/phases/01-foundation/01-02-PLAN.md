---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - cmd/kaptanto/main.go
  - internal/cmd/root.go
  - internal/cmd/root_test.go
  - Makefile
autonomous: true
requirements: [CFG-01, PRF-02]

must_haves:
  truths:
    - "Running `kaptanto --help` displays all CLI flags: --source, --tables, --output, --port, --config, --data-dir, --retention, --ha, --node-id"
    - "`go build ./...` succeeds with CGO_ENABLED=0"
    - "The binary is a single static executable with no CGO dependency"
    - "`kaptanto --log-level debug` configures structured JSON logging at debug level"
  artifacts:
    - path: "cmd/kaptanto/main.go"
      provides: "Binary entry point calling cmd.Execute()"
      min_lines: 10
    - path: "internal/cmd/root.go"
      provides: "Root cobra command with all CLI flags"
      contains: "PersistentFlags"
    - path: "Makefile"
      provides: "Build targets with CGO_ENABLED=0 verification"
      contains: "CGO_ENABLED=0"
  key_links:
    - from: "cmd/kaptanto/main.go"
      to: "internal/cmd/root.go"
      via: "calls cmd.Execute()"
      pattern: "cmd\\.Execute"
    - from: "internal/cmd/root.go"
      to: "internal/logging/logging.go"
      via: "PersistentPreRunE initializes logging from --log-level flag"
      pattern: "logging\\.Setup"
---

<objective>
Create the CLI entry point with all required flags and the Makefile with pure-Go build verification.

Purpose: Provide the `kaptanto` binary skeleton that all future phases will add subcommands and behavior to. Verify the pure-Go build constraint (PRF-02) that the entire project depends on.
Output: Working `kaptanto --help` binary and Makefile with build/test/verify targets.
</objective>

<execution_context>
@/Users/lucasandrade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/lucasandrade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

<interfaces>
<!-- From Plan 01 - logging package the CLI will use -->
From internal/logging/logging.go:
```go
func ParseLevel(s string) slog.Level
func Setup(w io.Writer, level string) *slog.Logger
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create CLI skeleton with cobra and all required flags</name>
  <files>cmd/kaptanto/main.go, internal/cmd/root.go, internal/cmd/root_test.go</files>
  <behavior>
    - Test: Execute() with --help returns no error and output contains "kaptanto"
    - Test: Root command has persistent flag "source" (string)
    - Test: Root command has persistent flag "tables" (string slice)
    - Test: Root command has persistent flag "output" with default "stdout"
    - Test: Root command has persistent flag "port" with default 7654
    - Test: Root command has persistent flag "config" (string)
    - Test: Root command has persistent flag "data-dir" with default "./data"
    - Test: Root command has persistent flag "retention" (duration)
    - Test: Root command has persistent flag "ha" (bool, default false)
    - Test: Root command has persistent flag "node-id" (string)
    - Test: Root command has persistent flag "log-level" with default "info"
  </behavior>
  <action>
    1. Create cmd/kaptanto/main.go:
       - package main, imports internal/cmd
       - main() calls cmd.Execute(), if error prints to stderr and os.Exit(1)
    2. Create internal/cmd/root.go:
       - package cmd
       - Define rootCmd as &cobra.Command with Use:"kaptanto", Short:"Universal database CDC", Long description
       - Add all persistent flags per CFG-01: --source (string), --tables ([]string), --output (string, default "stdout"), --port (int, default 7654), --config (string), --data-dir (string, default "./data"), --retention (duration, default 0 meaning 1h applied later), --ha (bool, default false), --node-id (string)
       - Add --log-level (string, default "info") for OBS-03
       - PersistentPreRunE: read --log-level flag, call logging.Setup(os.Stderr, logLevel) to initialize structured logging before any command runs
       - Export Execute() function that returns rootCmd.Execute()
       - Per research pitfall 5: use exact kebab-case flag names consistently
    3. Create internal/cmd/root_test.go:
       - Test that each flag exists with correct type and default
       - Test that --help output includes "kaptanto" and key flag names
    4. `go get github.com/spf13/cobra@latest`
    5. Write tests first (TDD), then implement.
  </action>
  <verify>
    <automated>cd /Users/lucasandrade/kaptanto && CGO_ENABLED=0 go test ./internal/cmd/... -v -count=1</automated>
  </verify>
  <done>All 11 CLI flags exist with correct types and defaults. --help output shows all flags. Logging is initialized via PersistentPreRunE.</done>
</task>

<task type="auto">
  <name>Task 2: Create Makefile and verify pure-Go build</name>
  <files>Makefile</files>
  <action>
    1. Create Makefile with targets:
       - `build`: CGO_ENABLED=0 go build -trimpath -ldflags="-s -w" -o kaptanto ./cmd/kaptanto
       - `test`: CGO_ENABLED=0 go test ./... -v -race -count=1 (note: -race requires CGO on some platforms, so use `go test ./... -v -count=1` without -race for CGO_ENABLED=0, and a separate `test-race` target with CGO enabled)
       - `test`: CGO_ENABLED=0 go test ./... -v -count=1
       - `verify-no-cgo`: CGO_ENABLED=0 GOOS=linux GOARCH=amd64 go build ./... && CGO_ENABLED=0 GOOS=darwin GOARCH=arm64 go build ./... with success echo
       - `clean`: rm -f kaptanto
       - .PHONY declarations for all targets
    2. Run `make build` to verify binary compiles
    3. Run `./kaptanto --help` to verify output shows all flags
    4. Run `make verify-no-cgo` to verify cross-compilation works
    5. Add "kaptanto" (the binary) to .gitignore if not already present
  </action>
  <verify>
    <automated>cd /Users/lucasandrade/kaptanto && make build && ./kaptanto --help && make verify-no-cgo</automated>
  </verify>
  <done>Makefile builds the binary with CGO_ENABLED=0. `kaptanto --help` displays all flags. Cross-compilation to linux/amd64 and darwin/arm64 succeeds.</done>
</task>

</tasks>

<verification>
Full phase verification:
```bash
cd /Users/lucasandrade/kaptanto
CGO_ENABLED=0 go test ./... -v -count=1
make build
./kaptanto --help
make verify-no-cgo
```
</verification>

<success_criteria>
- `kaptanto --help` displays all 11 flags with descriptions and defaults
- `make build` produces a static binary without CGO
- `make verify-no-cgo` cross-compiles for linux/amd64 and darwin/arm64
- All tests pass with CGO_ENABLED=0
- Structured JSON logging initializes from --log-level flag
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
