---
phase: 07.5-observability-hardening
plan: 02
subsystem: observability
tags: [healthz, health-probes, config, ha, node-id, shutdown, graceful-stop, sse, grpc]

# Dependency graph
requires:
  - phase: 07.5-01
    provides: Ping() methods on BadgerEventLog, SQLiteStore, SQLiteCursorStore; KaptantoMetrics wired

provides:
  - config.Config HA bool and NodeID string fields with yaml tags ha/node-id
  - config.Merge handles --ha and --node-id flags via Changed checks
  - NewHealthHandler receives four real probes: badger, checkpoint, cursors, postgres
  - SSE and gRPC observability HTTP shutdown goroutines bounded to 5s timeout
  - runPipeline logs slog.Warn when cfg.HA is true

affects: [08-ha-leader-election, future phases using health endpoint]

# Tech tracking
tech-stack:
  added: []
  patterns: [real health probes with named Check funcs, context.WithTimeout for bounded shutdown goroutines]

key-files:
  created: []
  modified:
    - internal/config/config.go
    - internal/cmd/root.go

key-decisions:
  - "Postgres health probe uses context.WithTimeout(2s) — prevents probe hanging on unreachable host while not blocking handler"
  - "HA warning emitted via slog.Warn in runPipeline immediately after startup log — visible in structured logs without failing startup"
  - "Shutdown contexts created inside goroutine (not outside) — context lifetime scoped to shutdown, not pipeline lifetime"

patterns-established:
  - "Bounded shutdown: context.WithTimeout(5s) inside ctx.Done() goroutine for HTTP servers"
  - "Health probe: HealthProbe{Name, Check} slice passed to NewHealthHandler at step 8 after all stores initialized"

requirements-completed: [OBS-02, CFG-01]

# Metrics
duration: 3min
completed: 2026-03-16
---

# Phase 7.5 Plan 02: Health Probes, Bounded Shutdown, HA Config Summary

**Four real /healthz probes (badger, checkpoint, cursors, postgres), 5s-bounded SSE/gRPC shutdown, and HA/NodeID config fields replacing dead --ha/--node-id flag stubs**

## Performance

- **Duration:** 3 min
- **Started:** 2026-03-16T18:39:00Z
- **Completed:** 2026-03-16T18:42:00Z
- **Tasks:** 1
- **Files modified:** 2

## Accomplishments
- `/healthz` now returns 503 + diagnostic JSON when any of four named probes fails (Badger, checkpoint SQLite, cursor SQLite, Postgres)
- SSE and gRPC observability HTTP shutdown goroutines use `context.WithTimeout(5s)` — server cannot hang indefinitely on connected clients
- `config.Config` gains `HA bool` and `NodeID string` fields with correct yaml tags; `config.Merge` reads both flags when explicitly set
- `runPipeline` logs `slog.Warn` when `cfg.HA == true` (Phase 8 reserved)

## Task Commits

1. **Task 1: Add HA/NodeID to Config and wire real health probes + bounded shutdown** - `f844867` (feat)

**Plan metadata:** (docs commit below)

## Files Created/Modified
- `internal/config/config.go` — Added `HA bool` (yaml:"ha") and `NodeID string` (yaml:"node-id") to Config struct; added `ha` and `node-id` cases to Merge
- `internal/cmd/root.go` — Replaced empty HealthProbe slice with four real probes; added cfg.HA warning log; fixed SSE and gRPC shutdown to use context.WithTimeout(5s)

## Decisions Made
- Postgres health probe uses `context.WithTimeout(2s)` to avoid hanging the /healthz handler when Postgres is unreachable
- HA warning emitted via `slog.Warn` (not error) — does not fail startup, operator-visible in structured logs
- Shutdown context created inside the goroutine body so its lifetime is scoped to the shutdown action, not the pipeline

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- OBS-01 (metric wiring), OBS-02 (real health probes), and CFG-01 (HA/NodeID flags backed by Config) are all satisfied
- Phase 7.5 observability hardening is complete
- Phase 8 (HA leader election) can now read `cfg.HA` and `cfg.NodeID` from config without plumbing changes

---
*Phase: 07.5-observability-hardening*
*Completed: 2026-03-16*
