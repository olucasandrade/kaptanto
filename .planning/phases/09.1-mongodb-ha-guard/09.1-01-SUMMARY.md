---
phase: 09.1-mongodb-ha-guard
plan: "01"
subsystem: ha
tags: [ha, mongodb, guard, error-handling, cli]

# Dependency graph
requires:
  - phase: 09-mongodb-connector
    provides: cfg.SourceType() auto-detection for mongodb:// and mongodb+srv:// prefixes
  - phase: 08-high-availability
    provides: HA election block in runPipeline with ha.NewLeaderElector
provides:
  - Early-return guard in runPipeline rejecting --ha + MongoDB source combination with clear error
  - INT-03 gap closure — actionable error instead of cryptic pgx driver error
affects: [09.1-mongodb-ha-guard]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "Guard-before-subsystem pattern: validate incompatible flag combinations before expensive operations (pgx connect)"

key-files:
  created: []
  modified:
    - internal/cmd/root.go
    - internal/cmd/root_test.go

key-decisions:
  - "Guard placed immediately after slog.Info startup log and before HA block — earliest possible rejection point"
  - "Error message template: 'ha: --ha requires a Postgres source DSN; MongoDB source detected (%s)' — prefixed ha:, mentions Postgres, echoes detected URI"
  - "cfg.SourceType() reused (Phase 09-03 decision) — no new detection logic needed"

patterns-established:
  - "TDD RED/GREEN for guard conditions: write failing test, confirm failure matches expected partial match, add guard, confirm all three assertions pass"

requirements-completed: [INT-03]

# Metrics
duration: 1min
completed: 2026-03-17
---

# Phase 09.1 Plan 01: MongoDB + HA Guard Summary

**Early-return guard in runPipeline rejects --ha + MongoDB source combination with actionable error before any pgx connection attempt (INT-03 gap closure)**

## Performance

- **Duration:** 1 min
- **Started:** 2026-03-17T01:34:47Z
- **Completed:** 2026-03-17T01:35:47Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Guard condition `cfg.HA && cfg.SourceType() == "mongodb"` inserted before HA block in `runPipeline`
- Error message contains "ha:", "Postgres", and detected source URI — all three assertions pass
- TDD RED/GREEN: test written first (confirmed pgx driver error lacked "Postgres"), guard added, test passes
- TestHAFlagSkipsWithoutDSN and TestMongoDBFlagRoute unaffected — no regressions

## Task Commits

Each task was committed atomically:

1. **Task 1: Add MongoDB + HA guard to runPipeline** - `b6cb447` (feat) — TDD RED + GREEN in single commit
2. **Task 2: Full test suite verification** - no code changes; build and full suite passed

**Plan metadata:** (docs commit follows)

_Note: TDD RED confirmed test failed because pgx parse error omitted "Postgres"; GREEN confirmed guard fires before ha.NewLeaderElector_

## Files Created/Modified
- `internal/cmd/root.go` - Guard condition added before HA block (line 219-222)
- `internal/cmd/root_test.go` - TestMongoDBWithHAReturnsError added (INT-03 gap closure)

## Decisions Made
- Guard placed before `var pgStore *checkpoint.PostgresStore` so it fires before any connection resource is allocated
- Error message echoes `cfg.Source` in the message body — operator can see which URI was rejected
- cfg.SourceType() reused from Phase 09-03 — no new detection logic

## Deviations from Plan

None — plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None — no external service configuration required.

## Next Phase Readiness
- INT-03 is closed. MongoDB + HA incompatibility now surfaces with an actionable error.
- Phase 09.1 Plan 01 complete. Ready for next plan in phase 09.1 or Phase 10.

---
*Phase: 09.1-mongodb-ha-guard*
*Completed: 2026-03-17*
