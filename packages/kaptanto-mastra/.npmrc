# Peer packages (mastra, @kaptanto/events) are declared but not installed for
# unit/integration tests — the trigger API is duck-typed and events is linked
# via the file: devDependency. Consumers install peers themselves.
auto-install-peers=false
omit=peer
