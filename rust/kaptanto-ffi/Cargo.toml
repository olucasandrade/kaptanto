[package]
name = "kaptanto-ffi"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["staticlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
fnv = "1"
libc = "0.2"

[build-dependencies]
cbindgen = "0.26"

[profile.release]
panic = "abort"
